import 'package:flutter/material.dart';

class DoctorAlertPage extends StatelessWidget {
  const DoctorAlertPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("alert"),);
  }
}
